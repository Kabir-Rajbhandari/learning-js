// learning function

// return function
function addNumber(x, y) {
  // (x,y) function parameters
  let sumValue = x + y;
  return sumValue;
}

console.log(addNumber(45, 50)); // (values) function arguments

function greet() {
  console.log("Namaste ");
}

greet();
